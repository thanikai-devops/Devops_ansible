@Library ("my_shared_library") _ 
pipeline{
    agent any

    environment {
        REPO_URL = "https://SciflareIT@bitbucket.org/dpl11-backend/goo-plus.git"
        GIT_BRANCH = "seran_dev"
        IMAGE_NAME = 'alpine'
        IMAGE_TAG = 'latest'
        // DOCKERFILE_PATH
    }

    stages{
        stage("Checkout-Code"){
            steps{
                script{
                    gitcheckout(gitbranch: env.GIT_BRANCH , repourl: env.REPO_URL)
                }
            }
        }
        // stage("Build Node"){
        //     steps{
        //         script{
        //             build()
        //         }
        //     }
        // }
    }
}

